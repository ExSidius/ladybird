[package]
name = "wasm-dom-rust-chain"
version = "0.1.0"
edition = "2021"

# Keep this crate out of the repository's Cranelift workspace.
[workspace]

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true
